import { Button } from '@mui/material';

const CustomButton = () => {
  return (
    <Button
      variant="contained"
      color="primary"
      sx={{ borderRadius: '12px', padding: '10px 20px' }}
    >
      Test React
    </Button>
  );
};

export default CustomButton;
